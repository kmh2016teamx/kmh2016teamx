<%- model_class = Lecture -%>
<div class="page-header">
  <h1>講義一覧</h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>講義名</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @lectures.each do |lecture| %>
      <tr>
        <td><%=link_to lecture.name, lecture_issues_path(lecture_id: lecture.id)%></td>
        <td><%= link_to '編集', lecture_path(lecture) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "新しい講義を作成する",
            new_lecture_path,
            :class => 'btn btn-primary' %>
